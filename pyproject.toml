[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "fetcher"
version = "0.1.0"
requires-python = ">=3.8"

[project.scripts]
fetcher = "main:main"
